# TODO

1. read a CSV / TSV file with the first line being the header line.
2. with these values, load the template md file and replace all the ${FIELD_NAME} values corresponding to the header values
3. generate a mark down file for from each row in the CSV file
4. for each generated mark down file, produce an email draft (if appropriate), replacing the url files with the
1. generate md files from a template md and replace all the ${FIELD_NAME}
